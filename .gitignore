# Secrets
.env

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# DuckDB (rebuilt from raw by dbt)
*.duckdb
*.duckdb.wal

# dbt (Phase 2)
dbt/target/
dbt/dbt_packages/
dbt/logs/

# Evidence build output (Phase 3)
dashboard/.evidence/
dashboard/node_modules/
dashboard/build/

# Tooling
graphify-out/
.DS_Store

# Claude Code kit : on versionne commands/ settings.json CLAUDE.md PROGRESS.md,
# mais pas les sauvegardes locales ni les overrides personnels
.claude/backups/
.claude/settings.local.json

# Raw source photos (processed once into dashboard/public/photos, which IS versioned)
/assets/

# NOTE: data/raw/ is intentionally VERSIONED (small Parquet files,
# daily commits are the audit trail that the pipeline ran during the tournament).

# Astro dashboard
dashboard/node_modules/
dashboard/dist/
dashboard/.astro/

# soccerdata scratch lockfiles (touched on import, not project output)
downloaded_files/
